{# templates/table/index.html.twig #}

{% extends 'base.html.twig' %}

{% block body %}


<table class="table">
    <thead>
        <tr>
            <th>Nom Prénom</th>
            <th>Statut</th>
            <th>Objectif</th>
            <th>Réalisation</th>
            <th>Progression</th>
            <th class="iconeOP"><img src="\images\icons\Positive Dynamic.svg" alt=""></th>
        </tr>
    </thead>
    <tbody>
        {% for user in users %}
        <tr>
            <td>{{ user.nom }} {{ user.prenom }}</td>
            <td>
                {% if 'ROLE_EXPERT' in user.roles %}
                    <span id="colEx">Expert</span>
                {% elseif 'ROLE_SENIOR' in user.roles %}
                    <span id="colSe">Senior</span>
                {% else %}
                    <span id="colAp">Apprenti</span>
                {% endif %}
            </td>
            {# <td class='objectiveColumn'>{{ objectiveUser }} €</td> #}
            <td> 
            {# {% for prixFacture in prixFactures %}
                <tr>
                <td>{{prixFacture.prix_ht}}</td>
                </tr>
               
            {% endfor %} #}
            </td>


            <td> <!-- Insérez la colonne "Progression" ici --> </td>

            <td class='operation'>
                {# {% set operationCount = 0 %}
                {% for operation in operations %}
                    {% if operation.user.id == user.id %}
                        {% set operationCount = operationCount + 1 %}
                    {% endif %}
                {% endfor %}
                {{ operationCount }}            #}
            </td>
        </tr>
        {% endfor %}
    </tbody>
</table>


{% endblock %}
